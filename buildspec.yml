version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - cd application-code/web-tier
      - npm install
  build:
    commands:
      - npm run build
artifacts:
  files:
    - appspec.yml
    - scripts/*
    - build/**/*
